# Systems
In an Entity–Component–System (ECS) architecture, a system is the part that contains the logic. It runs every frame (or on demand), finds entities that have the required components, and processes those components to update game behavior. In short:

A system is a piece of code that operates on all entities containing a specific set of components, implementing the behavior of the ECS.