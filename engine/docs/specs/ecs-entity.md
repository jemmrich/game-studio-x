# Entity

Entities are cheap, ID-only references—no behavior, no data.

## Definition
- A unique GUID representing a “thing” in the game.
- Stores no logic or state itself.
- Always lightweight (usually just a number).
