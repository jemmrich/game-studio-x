/**
 * Re-export Three.js so that consuming applications can import
 * Three.js through the engine rather than directly.
 * This ensures a single source of truth and simplifies dependency resolution.
 */
export * from "three";
